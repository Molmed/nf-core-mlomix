/*
========================================================================================
    Nextflow config file for running nf-test tests
========================================================================================
*/

params {
    // Local test data paths (no remote dependencies)
    max_cpus   = 2
    max_memory = '6.GB'
    max_time   = '6.h'
}

// Test-specific profiles
profiles {
    gex_only {
        params {
            input = 'tests/samplesheet_gex_only.csv'
            run_gex = true
            run_dnam = false
            annotation_version = 'v107'
        }
    }

    dnam_precomputed {
        params {
            input = 'tests/samplesheet_dnam_precomputed.csv'
            run_gex = false
            run_dnam = true
            use_precomputed_dnam = true
        }
    }

    dnam_idat {
        params {
            input = 'tests/samplesheet_dnam_idat.csv'
            run_gex = false
            run_dnam = true
            use_precomputed_dnam = false
        }
    }

    mixed_all_modes {
        params {
            input = 'tests/samplesheet_mixed_all_modes.csv'
            run_gex = true
            run_dnam = true
            annotation_version = 'v107'
        }
    }

    invalid_single_sample_mixed {
        params {
            input = 'tests/samplesheet_invalid_single_sample_mixed.csv'
            run_gex = false
            run_dnam = true
        }
    }
}
